# Next.js Running App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and push a public GitHub repository containing a minimal deployable Next.js app that displays `we are running`.

**Architecture:** Use a standard Next.js App Router project generated in the existing repository root. Keep the homepage as a single focused component and add a conventional multi-stage Dockerfile for production deployment.

**Tech Stack:** Next.js, React, TypeScript, npm, Node.js Alpine Docker image, GitHub CLI.

---

## File Structure

- Create/modify: `package.json` - npm scripts and dependencies generated by Next.js.
- Create/modify: `package-lock.json` - dependency lockfile.
- Create/modify: `app/page.tsx` - homepage that renders `we are running`.
- Create/modify: `app/layout.tsx` - default root layout generated by Next.js.
- Create/modify: `app/globals.css` - minimal global styling generated by Next.js.
- Create/modify: `next.config.ts` - standard Next.js config.
- Create/modify: `tsconfig.json` - TypeScript config generated by Next.js.
- Create: `Dockerfile` - multi-stage production Docker build.
- Create: `.dockerignore` - excludes local/generated files from Docker build context.
- Modify: `.gitignore` - standard Next.js ignores generated by scaffold.
- Preserve: `docs/superpowers/specs/2026-05-24-nextjs-running-app-design.md` - approved design spec.
- Preserve: `docs/superpowers/plans/2026-05-24-nextjs-running-app.md` - this plan.

### Task 1: Scaffold Next.js App

**Files:**
- Create/modify: `package.json`
- Create/modify: `package-lock.json`
- Create/modify: `app/layout.tsx`
- Create/modify: `app/page.tsx`
- Create/modify: `app/globals.css`
- Create/modify: `next.config.ts`
- Create/modify: `tsconfig.json`
- Modify: `.gitignore`

- [ ] **Step 1: Scaffold the app in the repository root**

Run:

```bash
npx create-next-app@latest . --ts --eslint --app --src-dir false --use-npm --import-alias "@/*" --yes
```

Expected: Next.js project files are created in `/home/emmanuel/work/test-app` without deleting `docs/`.

- [ ] **Step 2: Inspect generated files**

Run:

```bash
git status --short
```

Expected: new Next.js files appear as untracked or modified, and `docs/` remains present.

- [ ] **Step 3: Commit scaffold**

Run:

```bash
git add package.json package-lock.json app next.config.ts tsconfig.json .gitignore eslint.config.mjs public
git commit -m "Create Next.js app scaffold"
```

Expected: commit succeeds.

### Task 2: Implement Homepage Message

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: Replace homepage with required message**

Set `app/page.tsx` to:

```tsx
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <h1 className="text-4xl font-semibold tracking-tight">we are running</h1>
    </main>
  );
}
```

- [ ] **Step 2: Build the app**

Run:

```bash
npm run build
```

Expected: production build completes successfully.

- [ ] **Step 3: Commit homepage**

Run:

```bash
git add app/page.tsx
git commit -m "Show running status on homepage"
```

Expected: commit succeeds.

### Task 3: Add Docker Deployment Files

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`

- [ ] **Step 1: Add Dockerfile**

Create `Dockerfile` with:

```Dockerfile
FROM node:22-alpine AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

FROM node:22-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

FROM node:22-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
COPY package.json package-lock.json ./
COPY --from=deps /app/node_modules ./node_modules
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/public ./public
EXPOSE 3000
CMD ["npm", "start"]
```

- [ ] **Step 2: Add Docker ignore file**

Create `.dockerignore` with:

```gitignore
.git
.next
node_modules
npm-debug.log*
Dockerfile
.dockerignore
README.md
```

- [ ] **Step 3: Build the Docker image**

Run:

```bash
docker build -t nextjs-running-app .
```

Expected: Docker image builds successfully.

- [ ] **Step 4: Commit Docker files**

Run:

```bash
git add Dockerfile .dockerignore
git commit -m "Add Docker deployment configuration"
```

Expected: commit succeeds.

### Task 4: Create GitHub Repository And Push

**Files:**
- No file changes expected.

- [ ] **Step 1: Rename local branch to main**

Run:

```bash
git branch -M main
```

Expected: local branch is named `main`.

- [ ] **Step 2: Create public GitHub repository and push**

Run:

```bash
gh repo create nextjs-running-app --public --source=. --remote=origin --push
```

Expected: repository is created under the authenticated GitHub account and all commits are pushed to `origin/main`.

- [ ] **Step 3: Confirm remote**

Run:

```bash
gh repo view nextjs-running-app --json url --jq .url
```

Expected: prints the repository URL.
